# 파이썬에서 스택 자료구조는 다음과 같이 네 가지 기본 구조로 구현할 수 있다.
# 리스트 자료형
# .append() : 자료 넣기
# .pop() : 자료 빼기
# [-1] : 가장 위에 있는 자료 참조
# len() : 스택 크기 구하기

import sys

zeroList = []
numK = 0
tempNum = 0

numK = int(input(""))
for i in range(numK):
    tempNum = int(sys.stdin.readline())
    if tempNum == 0 :
        zeroList.pop()
    else :
        zeroList.append(tempNum)


print(sum(zeroList))



